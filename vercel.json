{
  "framework": null,
  "installCommand": "npm install && cd be-visible-google-ai-studio && npm install",
  "buildCommand": "rm -rf .next && npm run build && cd be-visible-google-ai-studio && npm run build",
  "outputDirectory": "be-visible-google-ai-studio/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
