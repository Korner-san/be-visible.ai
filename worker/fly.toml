# fly.toml app configuration file generated for worker-shy-waterfall-5527 on 2025-11-04T12:47:18+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'worker-shy-waterfall-5527'
primary_region = 'fra'

[build]

[http_service]
  internal_port = 10000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[env]
  PORT = "10000"
  NODE_ENV = "production"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
